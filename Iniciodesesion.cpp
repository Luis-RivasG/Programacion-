#include <iostream>

using namespace std;

int main()
{
    int contra = 0000;
    string user = user;
    bool loggin = false;
    int i =0; 

    while (i < 3)
    
    {
        cout << "Ingrese usuario";
        cin >> user;
        cout << "Ingrese contraseña";
        cin >> contra;

        {
            if (user == user && contra == contra)
            {
                cout << "Bienvenido al sistema" << endl;
                loggin = true;
            }
            else
            {
                cout << "Usuario y contrasña incorrectos";
            }
        }
        {
            while (loggin);
        }
    }
    
    return 0;
}
